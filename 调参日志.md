# 2026年6月4日
> 背景是 将k1机器人接入unilab进行训练，主要方式就是把g1替换为k1, 就本体的差异。
> 原来看k1仓库有amp的log，就让ai写amp的观测，没有对齐g1的观测，这个效果很不好，观测存在5帧历史信息，维度过大
1. 修改观测，观测对齐g1之后，只要1帧的数据，同时脚上的contact sensor增加到4个，效果改善很多
2. 对齐课程学习。发现k1训练前期的action_delta惩罚巨大，发现是没对齐g1的课程，前期要通过权重系数减少这个惩罚。对齐后，前期惩罚减小，策略收敛的更快了，效果显著
    像action 帧间差异这种希望机器人平滑的惩罚 确实刚开始时不应该很大

# 2026年6月5日 — K1SoccerDribble 带球 pose 惩罚虚高

> 背景：FlashSAC 训 `k1_soccer_dribble` 时，看板里 `pose` 显示 **−160 左右**，而同期 `k1_walk_flat` 只有 **−5 左右**，怀疑 reward 参数或 PenaltyCurriculum 没对齐。

## 现象

- 带球一阶段已对齐 walk 的 reward scale、curriculum（`penalty_scale=0.5` → 有效 `pose scale = −0.4`）、obs（walk 77 维 + phase + 球 3 维）、reset/cmd 采样。
- 看板 `pose` 仍约 **−163**；`penalty_action_rate` 也比 walk 大一个数量级。
- 用零动作 rollout 对比（Motrix，256 env，20 step）：
  - `k1_walk_flat`：`reward/pose ≈ −1.3`
  - `k1_soccer_dribble`（修复前）：`reward/pose ≈ −647`（raw 加权项 ~1600）

## 根因（不是 curriculum，是 default_angles 取错）

`LocomotionBaseEnv._init_buffers()` 里：

```python
self.default_angles = self._init_qpos[-self._num_action:]
```

- **平地** `scene_flat.xml` 的 keyframe `qpos` = base(7) + 机器人关节(22)，尾部 22 维就是关节角，正确。
- **带球** `scene_soccer_dribble_minimal.xml` 的 keyframe `qpos` = base(7) + 机器人(22) + **球 free-joint(7)**。
- 仍取 `[-22:]` 时，把 **球的 world 坐标**（如 `x=4.0, z≈0.09, quat=1…`）当成了关节 default。

实测 reset 后（修复前）：

| 关节 | qpos | 误用的 default |
|------|------|----------------|
| Left_Ankle_Roll | 0.0 | **4.0**（球的 x） |
| Right_Hip_Yaw | 0.0 | **1.0**（球的 qw） |

`pose` 奖励是 `Σ w_j (q_j − default_j)² × scale`，腿权重 200/120，default 错 4 rad → 单关节贡献上千 → 看板 `pose` 虚高 **两个数量级**。  
`apply_action` 的零动作目标也偏了，进一步放大步态/控制异常。

## 修复

1. **`constants.py`**：新增 `K1_KEYFRAME_BASE_QPOS_DIM = 7`、`k1_keyframe_robot_joint_qpos()`，从 keyframe 取 `[7 : 7+num_action]`，排除尾部球 qpos。
2. **`soccer_dribble.py`**：在 `K1SoccerDribbleEnv.__init__` 里覆盖 `self.default_angles`，不再用 `qpos[-22:]`。
3. **测试**：`test_soccer_default_angles_use_robot_keyframe_not_ball_tail` 防止回归。

## 验证（修复后，同脚本零动作对比）

| 任务 | Left_Ankle_Roll default | mean `reward/pose`（20 step） | pose scale |
|------|-------------------------|-------------------------------|------------|
| k1_walk_flat | 0.0 | **−0.70** | −0.4 |
| k1_soccer_dribble | 0.0 | **−0.76** | −0.4 |

与平地 walk 同量级；一阶段带球训练需 **重新开 run**（旧 checkpoint 在错误 default 下训的）。

## 同期一并做的带球对齐（非本 bug，但容易混淆）

- 去掉 AMP obs，改为 walk 布局 + `gait_phase` + 球 3 维（80 / 83 维）。
- 一阶段用 walk 的 DR reset / 随机 cmd（不再每步追 waypoint）；waypoint 只用于判定进 phase2。
- 关闭 warm-start；`motrix.yaml` 补齐 `noise_config`、curriculum；看板增加 `reward/penalty_scale`。

**涉及文件**：`src/unilab/envs/locomotion/k1/constants.py`、`soccer_dribble.py`、`tests/envs/locomotion/k1/test_k1_soccer_dribble.py`

## 附带：Motrix 训练崩溃（waypoint 可视化）

- **现象**：iter ~16680、`Ep Len ~179` 时在 `reset()` → `_sync_phase1_waypoint_visual` 崩溃：`motrixsim.Geom.local_pose` 不可写。
- **原因**：Motrix 后端 `set_world_geom_pos` 误实现为写 `geom.local_pose`；waypoint 绿/红球只是 **playback 标记**，不应在 Motrix 训练热路径改 geom。
- **修复**：Motrix 改抛 `NotImplementedError`；`soccer_dribble` 捕获后关闭 `_waypoint_visual_supported`，课程判定 `_phase1_reached`（机座到 waypoint 距离）不受影响。

## 一阶段 walk 对齐（2026-06-06）

- **ball obs**：phase1 置零，进 phase2（`phase1_reached`）后再写入真实 `rel_x/y/vx`；obs 维数仍为 80，与 walk 前 77 维 + 3 维零填充等价。
- **cmd**：`vel_limit` 改为 walk 同款 `[0.4,0,0]~[0.7,0,0]`（仅前向）。
- **步态门控**：`min_forward_speed_for_gait_reward: 0.05`（与 walk recipe 一致）。


## !!!重要发现
mujoco比motirx 在k1带球和平地走 训练更稳定

## 2026年6月7日 — 一阶段「到点 + 走直线 + 200 步截断」

> 背景：一阶段只有 walk 奖励时，策略容易磨蹭、绕路或迟迟不进 phase2。增加 **到 waypoint 的距离 shaping**、**偏离 +X 的速度惩罚**、**200 步未完成即终止**，三管齐下逼迫先完成一阶段。
>
> 验证 run：`2026-06-07_11-42-47_mujoco`（FlashSAC MuJoCo，20k iter）。末 iter 看板加权项（per-step mean，已含 scale×ctrl_dt）：
> - `phase1 reach` **+3.8449**
> - `penalty phase1 vel direction` **−0.0388**
> - `phase1 complete` +7.7925
> - `Timeout Rate` **63.3%**（含 phase1 200 步截断 + 其他终止前的 timeout 统计口径）

### 1. 一阶段关键点距离奖励 `phase1_reach`

| 项 | 值 |
|----|-----|
| raw | `1 - tanh(机座→waypoint 3D 距离 / σ)` |
| `phase1_reach_sigma` | **0.3 m** |
| scale | **10.0**（到达时 raw≈1 → 加权峰值约 **+10**/步量级，再 × `ctrl_dt=0.02`） |
| phase1 | 每步按距离实时计算，越近越大 |
| phase2 | **冻结**为刚进入 waypoint 那一刻的 reach 值，之后每步给固定值（不随走远下降） |

判定：机座到 waypoint 球心 3D 距离 ≤ **0.18 m**（原 0.08 m，+10 cm）。

### 2. 不走直线惩罚 `penalty_phase1_vel_direction`

| 项 | 值 |
|----|-----|
| 目标方向 | 世界系 **+X**（起点 → phase1 waypoint） |
| raw | 合速度横向分量 `‖v_xy - proj(v_xy, x̂)‖`（m/s） |
| 低速豁免 | 合速度 ≤ **0.05 m/s** 不罚 |
| scale | **−4.0** |
| phase1 | 100% 强度 |
| phase2 | **25%** 强度（`phase1_vel_direction_phase2_scale: 0.25` → 有效 scale **−1.0**） |

### 3. 一阶段 200 步截断 `term_phase1_timeout`

| 项 | 值 |
|----|-----|
| `phase1_max_steps` | **200**（原 600 → 500 → 200） |
| 条件 | `phase1_reached == False` 且 `steps >= 200` → `terminated` |
| 配套 DR | reset **x** 偏移 **±0.3 m**（原 ±0.5），最远到点 **1.3 m**；200 步 × 0.02 s × 0.4 m/s ≈ **1.6 m** 理论位移，余量更合理 |

### 一阶段 cmd（未改）

reset 采样 body 系 `[vx, vy, vyaw]`，整局不变：`vx ∈ [0.4, 0.7]`，`vy=0`，`vyaw=0`。

### 涉及文件

- `src/unilab/envs/locomotion/k1/soccer_dribble.py`（reward / 终止 / DR）
- `src/unilab/envs/locomotion/k1/constants.py`（步数、判定半径、DR 范围）
- `conf/offpolicy/task/flashsac/k1_soccer_dribble/mujoco.yaml`（及 motrix.yaml）
- `scripts/plot_phase1_reach_sigma.py`（σ 对比图）
- `conf/offpolicy/task/flashsac/k1_soccer_dribble/figures/phase1_reach_sigma.png`

**注意**：上述 YAML/代码改动需 **新开 train run**；`2026-06-07_11-42-47_mujoco` 为包含本套机制的训练结果。
