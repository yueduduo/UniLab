# 2026年6月4日
> 背景是 将k1机器人接入unilab进行训练，主要方式就是把g1替换为k1, 就本体的差异。
> 原来看k1仓库有amp的log，就让ai写amp的观测，没有对齐g1的观测，这个效果很不好，观测存在5帧历史信息，维度过大
1. 修改观测，观测对齐g1之后，只要1帧的数据，同时脚上的contact sensor增加到4个，效果改善很多
2. 对齐课程学习。发现k1训练前期的action_delta惩罚巨大，发现是没对齐g1的课程，前期要通过权重系数减少这个惩罚。对齐后，前期惩罚减小，策略收敛的更快了，效果显著
    像action 帧间差异这种希望机器人平滑的惩罚 确实刚开始时不应该很大

# 2026年6月5日 — K1SoccerDribble 带球 pose 惩罚虚高

> 背景：FlashSAC 训 `k1_soccer_dribble` 时，看板里 `pose` 显示 **−160 左右**，而同期 `k1_walk_flat` 只有 **−5 左右**，怀疑 reward 参数或 PenaltyCurriculum 没对齐。

## 现象

- 带球一阶段已对齐 walk 的 reward scale、curriculum（`penalty_scale=0.5` → 有效 `pose scale = −0.4`）、obs（walk 77 维 + phase + 球 3 维）、reset/cmd 采样。
- 看板 `pose` 仍约 **−163**；`penalty_action_rate` 也比 walk 大一个数量级。
- 用零动作 rollout 对比（Motrix，256 env，20 step）：
  - `k1_walk_flat`：`reward/pose ≈ −1.3`
  - `k1_soccer_dribble`（修复前）：`reward/pose ≈ −647`（raw 加权项 ~1600）

## 根因（不是 curriculum，是 default_angles 取错）

`LocomotionBaseEnv._init_buffers()` 里：

```python
self.default_angles = self._init_qpos[-self._num_action:]
```

- **平地** `scene_flat.xml` 的 keyframe `qpos` = base(7) + 机器人关节(22)，尾部 22 维就是关节角，正确。
- **带球** `scene_soccer_dribble_minimal.xml` 的 keyframe `qpos` = base(7) + 机器人(22) + **球 free-joint(7)**。
- 仍取 `[-22:]` 时，把 **球的 world 坐标**（如 `x=4.0, z≈0.09, quat=1…`）当成了关节 default。

实测 reset 后（修复前）：

| 关节 | qpos | 误用的 default |
|------|------|----------------|
| Left_Ankle_Roll | 0.0 | **4.0**（球的 x） |
| Right_Hip_Yaw | 0.0 | **1.0**（球的 qw） |

`pose` 奖励是 `Σ w_j (q_j − default_j)² × scale`，腿权重 200/120，default 错 4 rad → 单关节贡献上千 → 看板 `pose` 虚高 **两个数量级**。  
`apply_action` 的零动作目标也偏了，进一步放大步态/控制异常。

## 修复

1. **`constants.py`**：新增 `K1_KEYFRAME_BASE_QPOS_DIM = 7`、`k1_keyframe_robot_joint_qpos()`，从 keyframe 取 `[7 : 7+num_action]`，排除尾部球 qpos。
2. **`soccer_dribble.py`**：在 `K1SoccerDribbleEnv.__init__` 里覆盖 `self.default_angles`，不再用 `qpos[-22:]`。
3. **测试**：`test_soccer_default_angles_use_robot_keyframe_not_ball_tail` 防止回归。

## 验证（修复后，同脚本零动作对比）

| 任务 | Left_Ankle_Roll default | mean `reward/pose`（20 step） | pose scale |
|------|-------------------------|-------------------------------|------------|
| k1_walk_flat | 0.0 | **−0.70** | −0.4 |
| k1_soccer_dribble | 0.0 | **−0.76** | −0.4 |

与平地 walk 同量级；一阶段带球训练需 **重新开 run**（旧 checkpoint 在错误 default 下训的）。

## 同期一并做的带球对齐（非本 bug，但容易混淆）

- 去掉 AMP obs，改为 walk 布局 + `gait_phase` + 球 3 维（80 / 83 维）。
- 一阶段用 walk 的 DR reset / 随机 cmd（不再每步追 waypoint）；waypoint 只用于判定进 phase2。
- 关闭 warm-start；`motrix.yaml` 补齐 `noise_config`、curriculum；看板增加 `reward/penalty_scale`。

**涉及文件**：`src/unilab/envs/locomotion/k1/constants.py`、`soccer_dribble.py`、`tests/envs/locomotion/k1/test_k1_soccer_dribble.py`

## 附带：Motrix 训练崩溃（waypoint 可视化）

- **现象**：iter ~16680、`Ep Len ~179` 时在 `reset()` → `_sync_phase1_waypoint_visual` 崩溃：`motrixsim.Geom.local_pose` 不可写。
- **原因**：Motrix 后端 `set_world_geom_pos` 误实现为写 `geom.local_pose`；waypoint 绿/红球只是 **playback 标记**，不应在 Motrix 训练热路径改 geom。
- **修复**：Motrix 改抛 `NotImplementedError`；`soccer_dribble` 捕获后关闭 `_waypoint_visual_supported`，课程判定 `_phase1_reached`（机座到 waypoint 距离）不受影响。

## 一阶段 walk 对齐（2026-06-06）

- **ball obs**：phase1 置零，进 phase2（`phase1_reached`）后再写入真实 `rel_x/y/vx`；obs 维数仍为 80，与 walk 前 77 维 + 3 维零填充等价。
- **cmd**：`vel_limit` 改为 walk 同款 `[0.4,0,0]~[0.7,0,0]`（仅前向）。
- **步态门控**：`min_forward_speed_for_gait_reward: 0.05`（与 walk recipe 一致）。


## !!!重要发现
mujoco比motirx 在k1带球和平地走 训练更稳定

## 2026年6月7日 — 一阶段「到点 + 走直线 + 200 步截断」

> 背景：一阶段只有 walk 奖励时，策略容易磨蹭、绕路或迟迟不进 phase2。增加 **到 waypoint 的距离 shaping**、**偏离 +X 的速度惩罚**、**200 步未完成即终止**，三管齐下逼迫先完成一阶段。
>
> 验证 run：`2026-06-07_11-42-47_mujoco`（FlashSAC MuJoCo，20k iter）。末 iter 看板加权项（per-step mean，已含 scale×ctrl_dt）：
> - `phase1 reach` **+3.8449**
> - `penalty phase1 vel direction` **−0.0388**
> - `phase1 complete` +7.7925
> - `Timeout Rate` **63.3%**（含 phase1 200 步截断 + 其他终止前的 timeout 统计口径）

### 1. 一阶段关键点距离奖励 `phase1_reach`

| 项 | 值 |
|----|-----|
| raw | `1 - tanh(机座→waypoint 3D 距离 / σ)` |
| `phase1_reach_sigma` | **0.3 m** |
| scale | **10.0**（到达时 raw≈1 → 加权峰值约 **+10**/步量级，再 × `ctrl_dt=0.02`） |
| phase1 | 每步按距离实时计算，越近越大 |
| phase2 | **冻结**为刚进入 waypoint 那一刻的 reach 值，之后每步给固定值（不随走远下降） |

判定：机座到 waypoint 球心 3D 距离 ≤ **0.18 m**（原 0.08 m，+10 cm）。

### 2. 不走直线惩罚 `penalty_phase1_vel_direction`

| 项 | 值 |
|----|-----|
| 目标方向 | 世界系 **+X**（起点 → phase1 waypoint） |
| raw | 合速度横向分量 `‖v_xy - proj(v_xy, x̂)‖`（m/s） |
| 低速豁免 | 合速度 ≤ **0.05 m/s** 不罚 |
| scale | **−4.0** |
| phase1 | 100% 强度 |
| phase2 | **25%** 强度（`phase1_vel_direction_phase2_scale: 0.25` → 有效 scale **−1.0**） |

### 3. 一阶段 200 步截断 `term_phase1_timeout`

| 项 | 值 |
|----|-----|
| `phase1_max_steps` | **200**（原 600 → 500 → 200） |
| 条件 | `phase1_reached == False` 且 `steps >= 200` → `terminated` |
| 配套 DR | reset **x** 偏移 **±0.3 m**（原 ±0.5），最远到点 **1.3 m**；200 步 × 0.02 s × 0.4 m/s ≈ **1.6 m** 理论位移，余量更合理 |

### 一阶段 cmd（未改）

reset 采样 body 系 `[vx, vy, vyaw]`，整局不变：`vx ∈ [0.4, 0.7]`，`vy=0`，`vyaw=0`。

### 涉及文件

- `src/unilab/envs/locomotion/k1/soccer_dribble.py`（reward / 终止 / DR）
- `src/unilab/envs/locomotion/k1/constants.py`（步数、判定半径、DR 范围）
- `conf/offpolicy/task/flashsac/k1_soccer_dribble/mujoco.yaml`（及 motrix.yaml）
- `scripts/plot_phase1_reach_sigma.py`（σ 对比图）
- `conf/offpolicy/task/flashsac/k1_soccer_dribble/figures/phase1_reach_sigma.png`

**注意**：上述 YAML/代码改动需 **新开 train run**；`2026-06-07_11-42-47_mujoco` 为包含本套机制的训练结果。

## 2026年6月7日 — K1SoccerPenaltyKick 点球任务调参

> 背景：基于 `K1SoccerDribble` 的 `model_13000.pt` warm-start，新增 `K1SoccerPenaltyKick` 任务。目标是让 K1 从点球点后方助跑，右脚触球后把球踢进 sim_soccer 尺寸球门。训练使用 FlashSAC + MuJoCo。

### 初始现象：只会助跑，不进 kick phase

早期 run（如 `2026-06-07_18-27-14_mujoco`、`2026-06-07_20-08-36_mujoco`）主要表现为：

- `Ep Len` 接近 `runup_max_steps=300`，大部分 episode 卡在助跑阶段。
- `runup_reach`、`right_foot_to_ball` 有值，但 `runup_success=0`。
- `ball_goal_progress`、`ball_kick_speed`、`ball_to_goal`、`goal_scored` 长期为 0。
- 视频上机器人能靠近球，但右脚没有稳定进入触球阈值，因此 kick phase 样本稀缺。

判断：问题不是射门奖励不够，而是 replay buffer 里几乎没有“触球后踢球”的样本。

### 第一轮：助跑速度与奖励方向对齐

发现 `tracking_lin_vel` / `under_speed` 仍按随机采样 cmd 计算，和“最低速度达标后越快越好”的 `runup_speed` 冲突：

- 跑得比采样 cmd 快也会被 `tracking_lin_vel` 扣。
- cmd 抽到高值时，实际速度不低也会被 `under_speed` 罚。

修复：

- 助跑阶段 `tracking_lin_vel` 改为只惩罚低于课程最低速，不惩罚快于采样 cmd。
- 助跑阶段 `under_speed` 只相对课程最低速算缺口。
- 踢球阶段仍保留原 cmd 跟踪逻辑。

同时逐步调整过助跑相关 scale：

- `tracking_lin_vel: 30.0 -> 7.5`
- `runup_reach: 20.0 -> 6.67`
- `right_foot_to_ball: 20.0 -> 6.67`
- `alive: 5.0 -> 0.0`
- `term_runup_timeout: -10.0 -> -30.0`
- `right_foot_strike_dist: 0.22 -> 0.30`

效果：助跑超时和速度奖励不再强行诱导“跑满 300 步刷分”，但仍容易出现 kick 样本不足。

### 第二轮：靠参数不够，改训练分布

单纯加大 `ball_*` 奖励无效，因为没进 kick phase 时这些奖励永远是 0。于是改为从 env 层解决样本分布：

1. **近球起步 curriculum**
   - phase1：`runup_start_distance_phase1: [0.20, 0.35]`
   - phase2：`runup_start_distance_phase2: [0.35, 0.80]`
   - phase3：`runup_start_distance_phase3: [2.0, 2.0]`

2. **右脚靠近球进度奖励**
   - 新增 `right_foot_to_ball_progress`
   - raw = `prev_right_foot_ball_dist - current_right_foot_ball_dist`
   - 只在 runup phase 生效，鼓励右脚每步更接近球，而不是停在不远不近的位置。

3. **速度课程改为按成功率推进**
   - 开启 `runup_speed_success_curriculum_enabled`
   - 不再只按 iteration 自动加速。
   - 只有 `runup_success_rate_ema` 达标后才提高最低速度。

实测 phase1 reset 后：

| 指标 | 值 |
|------|----|
| robot-ball reset 距离 | min/mean/max ≈ `0.201 / 0.290 / 0.376` |
| right-foot-ball reset 距离 | min/mean/max ≈ `0.186 / 0.289 / 0.409` |
| `right_foot <= strike_dist(0.30)` | **约 56% env** |

这说明 phase1 能直接往 replay buffer 注入 kick phase 样本。

### 第三轮：近球 curriculum 晋级过快

新 run 里出现“刚开始能 kick，后面又没了”：

- `runup_success` 在 iter 40–120 很高。
- `ball_goal_progress` 在 iter 40–180 有值。
- iter 200 后基本归零。

判断：`promotion_updates=20` 在 4096 env 下太快，phase1 没巩固就被拉远。

调整：

```yaml
runup_start_distance_success_threshold: 0.15
runup_start_distance_promotion_updates: 20
runup_speed_success_threshold: 0.15
runup_speed_success_promotion_updates: 20
```

后续观察仍表明需要更长 phase1/phase2 停留时间；如果再次退化，建议继续加大 `promotion_updates`，或改为显式 `min_iterations_per_phase`。

### 第四轮：能触球后，出现“贴球不射门”

run `2026-06-07_21-41-49_mujoco` 到 4k–6k 后：

- `runup_success` 稳定大于 50。
- `term_runup_timeout=0`。
- `goal_scored` 有明显正值。
- `Ep Len` 降到 130 左右。

但 1-env deterministic 视频里仍常见“右脚贴球 / 轻推球”，没有明显摆腿射门。

判断：策略已学会进入 kick phase，但 `runup_success` 和贴球相关奖励仍允许“触球后待着”这种局部解。

修复：

1. **`right_foot_to_ball` 只在 runup phase 给**
   - 进入 kick phase 后该项归零。
   - 避免继续奖励“脚贴球”。

2. **ball/goal reward 拉回中等强度**

```yaml
ball_goal_progress: 40.0
ball_kick_speed: 10.0
ball_to_goal: 2.0
goal_scored: 200.0
```

此前曾把 `ball_goal_progress / ball_kick_speed / ball_to_goal` 降到 `8 / 2 / 0.4`，`goal_scored` 降到 `20`；后来发现过低，射门信号存在感不足。

### 当前结果：batch 指标好，单条视频仍不稳定

当前较好 run：`logs/flash_sac/K1SoccerPenaltyKick/2026-06-07_21-41-49_mujoco`

iter 15000 附近训练面板：

| 指标 | 大致值 | 解读 |
|------|--------|------|
| `Ep Len` | `~120–130` | 不再拖助跑，kick 后很快结束 |
| `runup_success` | `~51–53` | kick phase 稳定 |
| `goal_scored` | `~65–96` | batch 中有进球事件 |
| `ball_goal_progress` | `~7.6–8.2` | 球有明显前进 |
| `ball_kick_speed` | `~3.3–3.5` | 有踢球速度 |
| `ball_to_goal` | `~2.9` | 方向奖励有响应 |
| `term_runup_timeout` | `0` | 助跑问题已解决 |
| `term_ball_missed_goal` | 小负值 | 有少量射偏 |

已批量录制该 run 下每个 checkpoint 的 1-env 视频：

- `play_video_model_1000_1env.mp4`
- `play_video_model_2000_1env.mp4`
- ...
- `play_video_model_15000_1env.mp4`

观察：训练 batch 指标显示已打通“助跑 → 触球 → 踢球 → 进球”链路，但确定性 1-env 视频成功率仍不高，常见轻推/贴球。可能原因：

1. 训练日志的 `goal_scored` 是 event reward 均值，不等同于单条视频成功率。
2. SAC 训练时有探索和大量 env，play 时是 deterministic mean action，动作更保守。
3. `runup_success` 仍较大，可能继续奖励“待在 kick phase”。
4. 近球 curriculum 让 batch 成功，但 full-distance / 单初始状态泛化还需观察。

### 下一步建议

优先做验证，而不是继续盲目调 scale：

1. 录 `play_env_num=16` 的视频，判断 batch 里是否确实有进球，而不是只看 1-env。
2. 统计 deterministic 多环境 rollout 的终止原因：`goal_scored / term_ball_missed_goal / term_kick_timeout / term_low`。
3. 若仍贴球：把 `runup_success: 100 -> 30`，或代码改成只在触球后前 10–20 步给。
4. 若 full-distance 泛化差：给近球 curriculum 增加最小停留 iteration，例如 phase1 至少 1500 iter、phase2 至少到 4000 iter。

### 涉及文件

- `src/unilab/envs/locomotion/k1/soccer_penalty_kick.py`
- `src/unilab/envs/locomotion/k1/soccer_penalty_constants.py`
- `conf/offpolicy/task/flashsac/k1_soccer_penalty_kick/mujoco.yaml`
- `conf/offpolicy/task/flashsac/k1_soccer_penalty_kick/motrix.yaml`
- `tests/envs/locomotion/k1/test_k1_soccer_penalty_kick.py`

### 验证

点球任务相关测试：

```bash
uv run pytest tests/envs/locomotion/k1/test_k1_soccer_penalty_kick.py -q
```

最近结果：`20 passed`。
